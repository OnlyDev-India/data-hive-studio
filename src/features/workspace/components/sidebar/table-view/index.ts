/** The sidebar's table/collection catalog browser — tree rows, dialogs
 *  (create/drop schema/database/table), and the flat table-list-item view
 *  are all grouped here since they only ever compose together. */
export { TablesBrowser } from "./tables-view";
